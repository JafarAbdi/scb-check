# scb-check (binary wheel)

Packaging subproject that wraps the fully-static musl `scb-check` binary into a
wheel. Built by CI and published to the GitHub release; consumed by the
`scb-check` pre-commit hook at the repository root. See the repo root README.
